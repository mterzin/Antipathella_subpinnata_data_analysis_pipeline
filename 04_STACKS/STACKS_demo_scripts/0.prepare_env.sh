#!/bin/bash

# STEP 1: Create a directory for the analysis, and subdirectories.
# STEP 2: Gather sequencing files (in raw/).
# STEP 3: Write text files containing the barcodes (in info/).
# STEP 4: Write a text file specifying the populations (in info/).

# MATERIALS: Make sure the necessary software is installed (c.f. `check_installs.sh`).
